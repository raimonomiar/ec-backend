const authErrors = require('./authErrors');
const userErrors = require('./userErrors');
const categoryErrors = require('./categoryErrors');

module.exports = {
  authErrors,
  userErrors,
  categoryErrors,
};
